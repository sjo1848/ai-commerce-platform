{
  "name": "@ai-commerce-platform/agent-core",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "types:generate": "wrangler types",
    "typecheck": "npm run types:generate && tsc -p tsconfig.json --noEmit",
    "build": "npm run types:generate && tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "qa": "npm run typecheck && npm test",
    "wrangler:dry-run": "wrangler deploy --dry-run"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "wrangler": "4.127.1"
  }
}
