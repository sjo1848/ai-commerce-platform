# ACP 2.6.9-R2.8 — Real-Model Receptionist Staging E2E — Block Plan

Status: `ACTIVE / EXECUTION PLAN BACKED`
Current block: `R2.8.2 — STAGING READINESS`
Last closed block: `R2.8.1 — CONTRACT + STAGING CORPUS — CONTRACT_FROZEN`

Purpose: split R2.8 into small, auditable blocks before the Human Product Acceptance gate. This file is a planning/traceability artifact; it does not by itself close R2.8 as a whole.

## R2.8.1 — Contract + staging corpus — `CLOSED / CONTRACT_FROZEN`
- Frozen contract: `.orchestration/contracts/ACP-2.6.9-R2.8-REAL-MODEL-STAGING-E2E.md`.
- Frozen corpus: `.orchestration/evidence/ACP-2.6.9-R2.8-STAGING-CORPUS.md`.
- Exact natural-language scenarios, grounded behavior and hard PASS/FAIL rules are fixed.
- Staging-data policy is fixed: R2.8.2 must discover a synthetic future two-night window where room numbers 101 and 102 are both authoritatively available; no UUID-shaped guest commands and no silent substitution of easier rooms.
- Cleanup contract is fixed: every run-created booking must be authoritatively cancelled/inactive and both rooms restored, otherwise R2.8 remains blocked.
- Gate: `CONTRACT_FROZEN` — **PASS**.

## R2.8.2 — Staging readiness — `ACTIVE`
- Verify authorized baseline model, HMS staging, bindings, sessions, telemetry, audit and usage capture.
- Discover a safe synthetic future two-night window where rooms 101 and 102 are both available.
- Record canonical room IDs for verification only; do not expose them as required natural-language input.
- Gate: `STAGING_READY`.

## R2.8.3 — Natural read-only conversation
- Greeting/social continuity.
- `habitaciones para dos` without redundant guest clarification.
- Dates/guests across turns, corrections and durable memory.
- Availability and room-number/ordinal grounding.
- No mutations.
- Gate: `CONVERSATION_PASS`.

## R2.8.4 — Natural multi-room dialogue
- Natural selection of two rooms, including the original 101+102 class of request.
- Occupancy distribution when required.
- Explicitly probe the historical R2.4 prompt wording so it cannot suppress the now-authorized R2.5 composite capability.
- Gate: `MULTI_ROOM_DIALOGUE_PASS`.

## R2.8.5 — HITL + create
- Produce an exact grounded mutation plan.
- Present exact human approval.
- Execute approved multi-room create only after approval.
- Verify HMS truth, ownership, idempotency, audit and usage.
- Gate: `CREATE_PASS`.

## R2.8.6 — Cancellation + cleanup
- Exercise specific/all cancellation semantics as appropriate.
- Verify HMS inventory/booking state after cancellation.
- Restore staging to a clean state and record cleanup evidence.
- Gate: `CLEANUP_PASS`.

## R2.8.7 — Product-quality review
- Review the complete transcript for natural receptionist quality, redundant questions, grounded facts, latency, fallbacks and scripted/form-like behavior.
- No unsafe fact invention or payment/process invention.
- Gate: `QA_PASS`.

## R2.8.8 — Pre-Critic
- Reconcile contract, corpus, transcripts, HMS outcomes, telemetry and QA evidence.
- Gate: `PRE_CRITIC_PASS`.

## R2.8.9 — Independent Critic
- Fresh falsification review of real-model staging evidence and safety/product boundaries.
- Any open P0/P1/P2 returns the substage to bounded rework.
- Gate: `INDEPENDENT_CRITIC_PASS` with `P0/P1/P2 = 0/0/0`.

## R2.8.10 — Technical closure
- Final evidence artifact.
- Merge if R2.8 required code/config changes.
- Post-merge main CI.
- Converge `.orchestration/STATE.md`, `.orchestration/STATUS.json`, Drive tracker and Issue #20.
- Result: `R2.8 TECHNICAL_PASS / CLOSED` and R2.9 Human Product Acceptance becomes the only next gate.

## Hard boundary
R2.8 does not authorize production cutover, real customer data, payments, paid-resource expansion, WhatsApp requirement, broader autonomous writes or the second vertical. Phase 3 / Alquileres remains blocked until explicit human `ACCEPT` at R2.9.
